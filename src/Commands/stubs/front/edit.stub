<template>
  <model-form
    titulo="Edição de {{ title }}"
    rota="{{ route }}"
    rotaVoltar="{{ route }}"
    edicao
    @on-submit="onValidationSubmit"
  ></model-form>
</template>

<script>
import ModelForm from "./forms/Model"

export default {
  components: { ModelForm },
  methods: {
      onValidationSubmit (model, isValid) {
        this.ModelForm = model
      }
    }
}
</script>