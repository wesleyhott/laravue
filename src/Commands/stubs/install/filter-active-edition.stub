<?php

namespace App\Filters;

class ActiveEdition
{
    public function filter($builder, $value)
    {   
        return ctype_digit($value) ? $builder->where('active', 1) ->orWhere('id', $value) : $builder->where('active', 1); 
    }
}